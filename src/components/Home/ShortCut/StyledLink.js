import { Box } from "../../../styles";

const StyledLink = ({ text }) => {
  return (
    <Box width="15%" height="90%" color="silver" margin="0 1rem">
      {text}
    </Box>
  );
};

export default StyledLink;
